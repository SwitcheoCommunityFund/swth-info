import { Blockchain } from '../constants';
declare type WalletResult = Record<Blockchain.Neo | Blockchain.Ethereum | 'mnemonic', string>;
export declare class HDWallet {
    static newWallet(): WalletResult;
    static getWallet(mnemonic: string, skipValidate?: boolean): WalletResult;
    private static getKey;
    private static getWalletResult;
    private static getCoinType;
}
export {};
