import { AddressOptions } from "../../utils";
export declare abstract class AccountProvider<T extends AddressOptions = AddressOptions> {
    options: AddressOptions;
    readonly publicKey: string;
    readonly scriptHash: string;
    readonly displayAddress: string;
    protected constructor();
    static connect(): Promise<any>;
    configureAddress(_options: T): void;
    abstract privateKey(): Promise<string>;
    abstract sign(msg: string): Promise<string>;
}
