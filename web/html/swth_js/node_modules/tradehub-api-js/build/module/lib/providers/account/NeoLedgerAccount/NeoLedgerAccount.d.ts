import TransportWebUSB from '@ledgerhq/hw-transport-webusb';
import { AddressOptions } from "../../../utils";
import { AccountProvider } from "../AccountProvider";
export declare class NeoLedgerAccount extends AccountProvider {
    readonly ledger: TransportWebUSB;
    readonly publicKey: string;
    readonly scriptHash: string;
    readonly displayAddress: string;
    private static _connectPolling;
    private constructor();
    static connect(): Promise<NeoLedgerAccount>;
    /**
     * Used to try connecting with ledger, executes a public key request
     * on USB device to detect NEO app connection
     */
    private static tryConnect;
    configureAddress(options: AddressOptions): void;
    privateKey(): Promise<string>;
    sign(msg: string): Promise<string>;
    private static getUSBDevices;
    private useLedger;
}
